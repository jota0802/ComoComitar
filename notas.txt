formato de push
git clone 
git init
git add .
git commit -m ""
git branch -M (branch)
git pull origin --allow-unrelated-histories
git push origin (branch)
ou


é necessario colocar dependencias, entrar no packge.json 
depois abrir terminal:
npm install react-router-dom

depois em main coloque:
 import {createBrowserRoutes, RouterProvider} from 'react-router-dom'
